version: '3.8'

services:
  video-sharing-client:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '5173:3000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
